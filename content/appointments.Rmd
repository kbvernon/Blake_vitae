## `r fa("briefcase")` Appointments

```{r}

read_sheet(google_page_id, sheet = "appointments")  %>%
  mutate(
    x = glue::glue("{title}, {organization}"),
    y = ifelse(
      is.na(end), 
      glue::glue("{start}-present"),
      glue::glue("{start}-{end}")
    )
  ) %>% 
  select(x, y) %>% 
  kbl(
    escape = FALSE,
    col.names = NULL,
    align = c('l', 'r')
  ) %>%
  kable_styling(full_width = TRUE) %>%
  collapse_rows(columns = 1, valign = "top")

```